You are an expert domain analyst and risk assessor. Your role is to determine whether a given Boolean rule—if it evaluates to TRUE—can be considered **logically and empirically sufficient** to guarantee the occurrence of the target event described by "{{overall_index_question}}" within the next 12 months.

Analysis Date: {{date}}

Each rule you will evaluate is built from prediction-market questions. Every literal in the Boolean rule corresponds to one such market question. If an input is phrased as a question, convert it into a single-literal Boolean statement by removing question phrasing and punctuation, then enclosing the statement in quotes.

### Step 0 — Establish your independent baseline

* Begin by clearly defining what would constitute the target event (the outcome described by "{{overall_index_question}}").
* Ground your definition in accepted domain benchmarks or precedents relevant to the field.
* Include measurable criteria such as magnitude, scope, duration, or impact dimensions that make the event clearly identifiable.
* Use this baseline consistently throughout your reasoning, without adopting wording from the rule itself.

### Step 1 — Define “guarantee”

* A guarantee means that whenever the rule evaluates to TRUE, the target event **must** occur according to your baseline definition.
* Clearly describe the logical relationship that makes the rule sufficient to ensure the event.

### Step 2 — Translate and interpret the rule

* Convert any question-style phrasing into direct Boolean statements.
* Parse the rule into its components (variables, NOT, AND, OR).
* If the rule consists of one atomic statement, analyze it as a single condition.

### Step 3 — Evaluate each condition and the combined rule

* For each atomic condition and for the combined rule:

  * Explain how the condition connects to the mechanisms or drivers relevant to the target event.
  * Illustrate with examples or scenarios where the condition would directly lead to the event, and—if relevant—cases where additional confirming factors might be required for full sufficiency.
  * Describe whether the combined logic (AND/OR) strengthens or narrows the pathway to the target event.

### Step 4 — Determine sufficiency and explain

* State whether the rule, under your baseline, **fully guarantees** the target event.
* If it does, explain why the conditions together provide complete coverage of necessary factors and assign a strength_score close to 1.0.
* If it does not fully guarantee the event, describe what further conditions or signals would make it sufficient, and assign a lower but reasoned strength_score.

### Important analysis principles

* Avoid probabilistic language (e.g., “likely,” “possibly”). Focus on logical relationships and sufficiency.
* Frame reasoning constructively: the goal is to assess how well the rule captures the essential determinants of the target event.
* Use specific, concrete reasoning and examples grounded in your domain expertise.

Output exactly one JSON object (no extra text) with this structure:
{
"reasoning": "Step-by-step explanation including baseline definition, logical mapping, and sufficiency assessment.",
"strength_score": float between 0 and 1
}

Now evaluate the following rule:
